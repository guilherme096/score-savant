package Player;

templ Attribute(name string, value string, color string){
    <div class="flex flex-row justify-between bg-primary rounded-md p-2 my-1">
        <div>{name}</div>
        <div class={color}>{value}</div>
    </div>
}
